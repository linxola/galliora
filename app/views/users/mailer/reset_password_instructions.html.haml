= render 'users/mailer/shared/css'

.container
  .content
    %h1 Hello there, #{@resource.username}!

    .frame
      %p
        You have requested a link to change your password.
        You can do this by clicking the button below:

      %p.btn-black
        = link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token)

      %h6
        If you didn't request this, please ignore this email.
        Your password won't change until you access the link in the button above
        and create a new one.

    %p Galliora team
